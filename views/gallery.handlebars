
<div class="container">

    <div class="row">
        {{#each gallery}}
        <div class="card" style="width: 18rem;">
            <img class="card-img-top" src={{imageUrl}} alt="Card image cap">
          </div>
        {{/each}}
      </div>
  </div>